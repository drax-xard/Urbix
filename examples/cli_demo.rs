//! Basic usage examples for the Urbix engine.
//!
//! This directory holds small, runnable programs that show consumers how to
//! drive the engine and demonstrate the API end-to-end.
//!
//! Planned examples:
//!
//! - `cli_demo.rs`     — walk a small grid and dump chunk summaries.
//! - `render_stub.rs`  — minimal consumer that reads the binary chunk layout.
//!
//! A companion `basic_usage.c` is added under `examples/` once the FFI surface
//! exists (Milestone 5) to prove C interop.

// TODO(Milestone 4+): implement the walk-grid demo once the API stabilizes.

fn main() {
    // Placeholder: the example is a no-op until the engine API exists.
    println!("Urbix CLI demo — to be implemented in Milestone 4+");
}
