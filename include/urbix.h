/* include/urbix.h
 *
 * Generated C header for the Urbix engine FFI.
 *
 * This file is produced automatically by `cbindgen` from the `#[no_mangle]
 * extern "C"` declarations in `src/ffi.rs` (wired through `build.rs`). It is
 * checked into the repository so that C, C++, and other FFI consumers can
 * depend on it directly without running a build step first.
 *
 * It declares the opaque engine handle, the `repr(C)` data records
 * (`UrbixCell`, `UrbixChunkHeader`, ...), the engine lifecycle functions, and
 * the chunk generation / zone query / configuration entry points.
 *
 * See `Urbix_Project.md` §2.3 and §2.4 for the wire format and API reference.
 *
 * This file is a placeholder until Milestone 5 wires up cbindgen.
 */
#ifndef URBIX_H
#define URBIX_H

#endif /* URBIX_H */
