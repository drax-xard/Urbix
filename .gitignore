# Rust build artifacts
/target
Cargo.lock

# Release packaging staging (CI builds artifacts under dist/)
/dist

# Micro-server binary (rebuilt via 3d-explorer-sdk/server/build.sh)
/3d-explorer-sdk/server/serve

# macOS
.DS_Store

# Visualizer output
out.png
out.ppm
